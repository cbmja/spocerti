<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spo.certificate.test.subject.mapper.SubjectMapper">

    <!--select-->

    <select id="findByCode" parameterType="int" resultType="Subject">
        SELECT * FROM subject WHERE code = #{code}
    </select>

    <select id="findAll" resultType="Subject">
        SELECT * FROM subject
    </select>
    <!--select-->

</mapper>