<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spo.certificate.board.mapper.BoardDataMapper">

<!--insert-->
    <insert id="save" parameterType="BoardData">
        INSERT INTO board_data (title , content , user_id , created_at , view_cnt , board_id , likes)
        VALUES(#{title} , #{content} , #{userId} , #{createdAt} , #{viewCnt} , #{boardId} , #{likes})
    </insert>
<!--insert-->
</mapper>