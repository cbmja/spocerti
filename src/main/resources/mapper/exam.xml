<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spo.certificate.test.exam.mapper.ExamMapper">

<!--select-->

    <select id="findByCode" parameterType="int" resultType="Exam">
        SELECT * FROM exam WHERE code = #{code}
    </select>

    <select id="findAll" resultType="Exam">
        SELECT * FROM exam
    </select>
<!--select-->

</mapper>